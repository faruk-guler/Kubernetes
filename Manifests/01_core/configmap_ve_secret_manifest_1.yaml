# app-config isimli ConfigMap nesnesi tanımlama dosyası
apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  # Basit key-value çiftleri
  LOG_LEVEL: "info"
  UI_THEME: "dark"
  # Tüm bir dosyayı içeriğe gömme
  settings.json: |
    {
      "max_retries": 5,
      "timeout": 30
    }
