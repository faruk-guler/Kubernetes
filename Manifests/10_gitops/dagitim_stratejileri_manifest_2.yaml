# api-service isimli Service nesnesi tanımlama dosyası
apiVersion: v1
kind: Service
metadata:
  name: api-service
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    # Trafiği nereye göndereceğimizi buradaki etiket belirler:
    version: blue
